[project]
name = "ai-dev"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "vvy-max", email = "13600226347@139.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
]

[project.scripts]
ai-dev = "ai_dev:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
